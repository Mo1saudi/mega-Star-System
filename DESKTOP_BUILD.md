# دليل بناء تطبيق ميجا ستار لسطح المكتب (Windows Executable .exe)

نظام **ميجا ستار - إدارة عمليات العمرة** مصمم للعمل كنظام ويب حديث ومستقل، وكذلك كتطبيق سطح مكتب ناطق بالنظام المباشر لـ Windows (.exe).

---

## 🛠️ متطلبات التشغيل والتطوير

1. **Node.js**: الإصدار 18 أو أحدث.
2. **npm**: الإصدار 9 أو أحدث.

---

## 🚀 تشغيل التطبيق في وضع سطح المكتب المحلي (Dev Mode)

لتشغيل خادم التطبيق مع نافذة Electron:

```bash
npm run electron:dev
```

---

## 📦 حزم وإنتاج ملف التثبيت النهائي (.exe)

لإنشاء برنامج التثبيت `Setup.exe` والنسخة المحمولة `Portable.exe`:

```bash
npm run electron:build
```

سيتم إنشاء المخرجات داخل مجلد `release/`:
1. `ميجا ستار - إدارة عمليات العمرة Setup 1.0.0.exe` (برنامج تثبيت عادي)
2. `ميجا ستار - إدارة عمليات العمرة 1.0.0.exe` (نسخة محمولة بدون تثبيت)

---

## 📁 المجلدات والمستودعات المحلية للبيانات (AppData)

يقوم تطبيق سطح المكتب بإنشاء المجلدات التالية تلقائيًا تحت مسار `AppData/Roaming/ميجا ستار - إدارة عمليات العمرة`:
- `Database/` - قاعدة البيانات المحلية (SQLite / IndexedDB)
- `UploadedPassports/` - صور جوازات السفر المرفوعة
- `ImportedExcel/` - ملفات إكسل المستوردة من الصفا
- `GeneratedPDFs/` - التقارير وقوائم السفر المطبوعة
- `Logs/` - سجلات العمليات والأخطاء
- `Backups/` - النسخ الاحتياطية التلقائية للنظام

---

## 🔒 حماية وأمان التطبيق

- تم تفعيل Context Isolation.
- تعطيل Node Integration في واجهة العرض (Renderer).
- دعم الاختصارات وعزل الروابط والتراجع والتكرار (Undo/Redo) لـ 50 خطوة.
